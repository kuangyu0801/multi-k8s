apiVersion: apps/v1
kind: Deployment
metadata:
  name: server-deployment
spec:
  # deployment create replicas set
  replicas: 3
  selector:
    matchLabels:
      component: server
  template:
    metadata:
      labels:
        component: server
    spec:
      containers:
        - name: server
          image: kuangyu0801/multi-server
          ports:
            - containerPort: 5000